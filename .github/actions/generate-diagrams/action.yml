name: Generate Architecture Diagrams
description: Generates/updates architecture diagrams using OpenAI and SKILL.md rules

inputs:
  openai_api_key:
    description: OpenAI API key
    required: true
  github_token:
    description: GitHub token for committing back
    required: true
  head_ref:
    description: PR head branch
    required: true
  base_sha:
    description: Base commit SHA
    required: true
  comment_body:
    description: Comment that triggered the action (if any)
    required: false
    default: ''

runs:
  using: composite
  steps:
    - uses: actions/checkout@v4
      with:
        ref: ${{ inputs.head_ref }}
        fetch-depth: 0

    - name: Run diagram review
      shell: bash
      env:
        OPENAI_API_KEY: ${{ inputs.openai_api_key }}
        GITHUB_TOKEN: ${{ inputs.github_token }}
        BASE_SHA: ${{ inputs.base_sha }}
        REVIEWER_COMMENT: ${{ inputs.comment_body }}
      run: bash ${{ github.action_path }}/review.sh
